export class Rol {
    public cod_rol: number;
    public detalle_rol: string;


    constructor(cod_rol: number, detalle_rol: string) {
        this.cod_rol = cod_rol;
        this.detalle_rol = detalle_rol;

    }
}
